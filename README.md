# dotfiles

## Quick starting

```
$ git clone git@github.com:tsub/dotfiles.git
$ cd dotfiles
$ /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
$ brew tap Homebrew/bundle
$ brew bundle
$ bin/setup
$ bin/mitamae local roles/develop.rb
```
